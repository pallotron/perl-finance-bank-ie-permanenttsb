- balance_graph(accountname, from, to, fileoutput)
  uses gnuplot to graph

- main shell command called ptsb to do operation
  on a permanent tsb account
  it works also as an example of usage

- test units
