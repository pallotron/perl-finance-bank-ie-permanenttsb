- convert to OOP rather than functional programming

- ~/.ptsb should be gpg encrypted

- balance_graph(accountname, from, to, fileoutput)
  uses gnuplot to graph

- main shell command called ptsb to do operation
  on a permanent tsb account
  it works also as an example of usage

- test units (using Mock Objects)

- check on login for invalid credentials
  
  - wrong username
  - wrong password
  - wrong pan

- log off problem
